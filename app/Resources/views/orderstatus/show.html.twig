{% extends 'base.html.twig' %}

{% block body %}
    <h1>Orderstatus</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ orderStatus.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ orderStatus.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('orderstatus_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('orderstatus_edit', { 'id': orderStatus.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
